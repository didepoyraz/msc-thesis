#version 450

layout(set = 0, binding = 0) buffer MatrixA {
   float dataA[];
};

layout(binding = 1) buffer MatrixB {
   float dataB[];
};

layout(binding = 2) buffer MatrixC {
   float output_dataC[];
};

layout (constant_id = 0) const uint MATRIX_SIZE = 1024;
layout (constant_id = 1) const uint TILE_SIZE = 16; 
layout (constant_id = 3) const uint THREADS_PER_TILE = 8;

//layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout (local_size_x = 8, local_size_y = 1, local_size_z = 1) in; // each thread will calculate a 2x2 output

uint RESULTS_PER_THREAD = TILE_SIZE / THREADS_PER_TILE;

shared float tileA[TILE_SIZE][TILE_SIZE];
shared float tileB[TILE_SIZE][TILE_SIZE];

void main() {
   uint globalRowBase = gl_GlobalInvocationID.x;
   uint globalColBase = gl_GlobalInvocationID.y;

   uint localRow = gl_LocalInvocationID.x;
   uint localCol = gl_LocalInvocationID.y;

   output_dataC[globalRowBase * MATRIX_SIZE + globalColBase] += 1;

   // // uvec2 gid = gl_GlobalInvocationID.xy;
   // // float resul
   // //  // each thread handles a TILE / RESULTS_PER_THREAD tile
   // //  // compute the starting cell for this tile
   // // uint base_x = gid.x* RESULTS_PER_THREAD;
   // // uint base_y = gid.y*RESULTS_PER_THREAD;
   // // uint globalRowBase = gl_GlobalInvocationID.x;
   // // uint globalColBase = gl_GlobalInvocationID.y;

   // uint localRow = gl_LocalInvocationID.x;
   // uint localCol = gl_LocalInvocationID.y;

   // uint group_id_x = gl_WorkGroupID.x;
   // uint group_id_y = gl_WorkGroupID.y;

   // uint globalRowBase = TILE_SIZE * group_id_x + localRow;
   // uint globalColBase = TILE_SIZE * group_id_y + localCol * RESULTS_PER_THREAD;

   // float result[RESULTS_PER_THREAD];
   // for (uint i = 0; i < RESULTS_PER_THREAD; ++i) {
   //       result[i] = 0.0;
   // }

  
   // uint num_tiles = (MATRIX_SIZE * MATRIX_SIZE )/ (TILE_SIZE*TILE_SIZE);

   // for (uint k = 0; k < num_tiles; ++k) {
   //    for (uint i = 0; i < RESULTS_PER_THREAD; ++i) {
   //       uint offsetCol = localCol * RESULTS_PER_THREAD + i;

   //       if (globalRowBase < MATRIX_SIZE && (k + offsetCol) < MATRIX_SIZE) {
   //          tileA[localRow][offsetCol] = dataA[globalRowBase * MATRIX_SIZE + (k + offsetCol)];
   //       }

   //       if ((k + localRow) < MATRIX_SIZE && (globalColBase + i) < MATRIX_SIZE) {
   //          tileB[localRow][offsetCol] = dataB[(k + localRow) * MATRIX_SIZE + (globalColBase + i)];
   //       }
   //    }

   //    barrier();

   //    for (uint t = 0; t < TILE_SIZE; ++t) {
   //       for (uint i = 0; i < RESULTS_PER_THREAD; ++i) {
   //          uint col = localCol * RESULTS_PER_THREAD + i;
   //          result[i] += tileA[localRow][t] * tileB[t][col];
   //       }
   //    }

   //    barrier();
   // }

   // for (uint i = 0; i < RESULTS_PER_THREAD; ++i) {
   //    uint globalCol = globalColBase + i;
   //    if (globalRowBase < MATRIX_SIZE && globalCol < MATRIX_SIZE) {
   //       output_dataC[globalRowBase * MATRIX_SIZE + globalCol] = result[i];
   //    }
   // }
}
